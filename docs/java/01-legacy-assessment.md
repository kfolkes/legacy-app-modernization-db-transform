# Phase 1 — Legacy Assessment (Java)

> Generated by `/java.modernize` orchestrator. Source: `legacy/java-asset-manager/`.

## Detected source profile

| Attribute | Value | Evidence |
|---|---|---|
| JDK | **8** | `pom.xml` → `<java.version>8</java.version>` |
| Build tool | **Maven 3** (multi-module) | parent + `web/` + `worker/` |
| Framework | **Spring Boot 2.7.18** (EOL Nov 2025) | parent `spring-boot-starter-parent` 2.7.18 |
| Module 1 | `assets-manager-web` (15 `.java` files) | Spring MVC, Thymeleaf, JPA, AMQP, AWS S3 |
| Module 2 | `assets-manager-worker` (11 `.java` files) | Spring Boot starter, AMQP, JPA, AWS S3 |
| Persistence | **JPA + Hibernate 5** on **PostgreSQL** | `spring-boot-starter-data-jpa`, `postgresql` runtime |
| Messaging | **RabbitMQ** (AMQP) — queues `image-processing`, `image-processing.retry` | `spring-boot-starter-amqp`, `RabbitConfig.java` |
| Object storage | **AWS S3** via AWS SDK v2 (2.25.13) | `software.amazon.awssdk:s3`, `AwsS3Config.java`, `AwsS3Service.java`, `S3FileProcessingService.java` |
| Logging | Spring Boot defaults (Logback) | inferred |
| Auth | Password-based for S3, RabbitMQ, PostgreSQL | `application.properties` plain creds |
| Container/deploy | None | no `Dockerfile`, no `azure.yaml` |

## Baseline legacy build (verification)

Built the legacy source as-is, under its declared JDK 8 (Adoptium Temurin 8u492), with Maven 3.9.0:

```text
[INFO] Reactor Summary for assets-manager-parent 0.0.1-SNAPSHOT:
[INFO] assets-manager-parent .............................. SUCCESS [  1.574 s]
[INFO] assets-manager-web ................................. SUCCESS [ 53.297 s]
[INFO] assets-manager-worker .............................. SUCCESS [  7.454 s]
[INFO] BUILD SUCCESS
```

> The legacy source compiles cleanly on JDK 8; one deprecation warning in `WebMvcConfig.java` (uses `WebMvcConfigurerAdapter`, removed in Spring 6 — see Phase 3).

## Detected migration hotspots (cross-validated)

| Hotspot | Tool 1 (`pom.xml` parse) | Tool 2 (source scan) |
|---|---|---|
| `javax.*` → `jakarta.*` | Spring Boot 2.x baseline | 5 `.java` files use `javax.persistence`, `javax.servlet`, `javax.annotation` |
| Removed `WebMvcConfigurerAdapter` / `HandlerInterceptorAdapter` | — | `web/.../config/WebMvcConfig.java` |
| AWS S3 client surface | `software.amazon.awssdk:s3` | `AwsS3Config`, `AwsS3Service`, `S3FileProcessingService`, `S3Controller`, `S3StorageItem` |
| RabbitMQ surface | `spring-boot-starter-amqp` | `RabbitConfig` (web + worker), `BackupMessageProcessor` |
| Password auth | `application.properties` | hardcoded `aws.accessKey`, `spring.rabbitmq.password`, `spring.datasource.password` |

## Outputs of this phase

- ✅ This document.
- ✅ Confirmed legacy still builds on JDK 8 (baseline for Phase 5 / 6 deltas).
